{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["turbo.json", "yarn.lock", "stylelint.config.cjs", "knip.ts", "Dockerfile", ".prettierrc"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "eslint": {
      "inputs": ["$TURBO_DEFAULT$", "yarn.lock"]
    },
    "tsc": {
      "inputs": ["$TURBO_DEFAULT$", "yarn.lock"]
    },
    "stylelint": {
      "inputs": ["$TURBO_DEFAULT$", "yarn.lock", "stylelint.config.cjs"]
    },
    "prettier": {
      "inputs": ["$TURBO_DEFAULT$", "yarn.lock"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "build-storybook": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build", "eslint", "tsc", "stylelint"],
      "outputs": ["{projectRoot}/dist"]
    },
    "clean": {
      "cache": false
    }
  }
}
