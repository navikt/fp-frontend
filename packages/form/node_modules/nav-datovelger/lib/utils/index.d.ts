import { DayPickerProps, Modifier } from 'react-day-picker';
import { DatepickerLimitations } from '../types';
export declare const dayDateKey: (date: Date) => string;
export declare const getInvalidDates: (limitations: DatepickerLimitations) => Modifier[];
export declare const isSameDate: (d1: Date, d2: Date) => boolean;
export declare const getDefaultMonth: (dateString: string | undefined, limitations: DatepickerLimitations | undefined, dayPickerProps: DayPickerProps | undefined) => Date;
