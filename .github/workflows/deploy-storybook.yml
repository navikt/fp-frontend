name: 'Deploy storybook'

on:
  push:
    branches: ['storybook-deploy-fix2']

jobs:
  deploy-storybook:
    permissions:
      contents: write
      pages: write
    uses: navikt/fp-frontend/.github/workflows/deploy-storybook-new.yml@storybook-deploy-fix2
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
