name: 'Deploy storybook'

on:
  push:
    branches: ['master']

jobs:
  deploy-storybook:
    permissions:
      contents: write
      pages: write
    uses: navikt/fp-gha-workflows/.github/workflows/deploy-storybook-new.yml@master
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
