name: "Deploy storybook"

on:
  push:
    branches: [ "master" ]
  schedule:
    - cron: '0 0 * * *'

jobs:
  deploy-storybook:
   uses: navikt/fp-gha-workflows/.github/workflows/deploy-storybook.yml@action/cleanup
   secrets: inherit
