name: "Deploy storybook"

on:
  schedule:
    - cron: '0 12 * * *'

jobs:
  deploy-storybook:
   permissions:
     actions: read
     contents: read
     pages: write
   uses: navikt/fp-gha-workflows/.github/workflows/deploy-storybook.yml@main
   secrets: inherit
