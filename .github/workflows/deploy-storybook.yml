name: 'Deploy storybook'

on:
  push:
    branches: ['test-turbo3']

jobs:
  deploy-storybook:
    permissions:
      contents: write
      pages: write
    uses: navikt/fp-frontend/.github/workflows/deploy-storybook-new.yml@test-turbo3
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
