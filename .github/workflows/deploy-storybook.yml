name: 'Deploy storybook'

on:
  push:
    branches: ['master']

jobs:
  deploy-storybook:
    permissions:
      contents: write
      pages: write
    uses: navikt/fp-gha-workflows/.github/workflows/deploy-storybook.yml@d82a6fe449f56938a073da20baca50bed596a800
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
