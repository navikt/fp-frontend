name: 'Deploy storybook'

on:
  push:
    branches: ['master']

jobs:
  deploy-storybook:
    permissions:
      contents: write
      pages: write
    uses: ./.github/workflows/deploy-storybook-new.yml
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
