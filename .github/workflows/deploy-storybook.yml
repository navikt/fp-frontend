name: 'Deploy storybook'

on:
  push:
    branches: ['storybook-turbo-cache']

jobs:
  deploy-storybook:
    permissions:
      contents: write
      pages: write
    uses: navikt/fp-frontend/.github/workflows/deploy-storybook-new.yml@storybook-turbo-cache
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
