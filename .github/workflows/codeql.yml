name: 'CodeQL'

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']
  schedule:
    - cron: '0 0 * * 0,4'

jobs:
  codeql:
    name: CodeQl
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: navikt/fp-gha-workflows/.github/workflows/codeql.yml@d82a6fe449f56938a073da20baca50bed596a800
    with:
      language: javascript
    secrets:
      READER_TOKEN: ${{ secrets.READER_TOKEN }}
